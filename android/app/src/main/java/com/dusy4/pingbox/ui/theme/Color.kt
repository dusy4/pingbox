package com.dusy4.pingbox.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * PingBox color palette — translated 1:1 from design_mobile_frontend/globals.css
 * Dark-first design system (Revolut-style)
 */

// Core background/foreground
val Background = Color(0xFF000000)          // --background: #000000
val Foreground = Color(0xFFFFFFFF)          // --foreground: #ffffff

// Primary action (white buttons on dark bg)
val Primary = Color(0xFFFFFFFF)             // --primary: #ffffff
val PrimaryForeground = Color(0xFF000000)   // --primary-foreground: #000000

// Secondary
val Secondary = Color(0xFF262626)           // --secondary: #262626
val SecondaryForeground = Color(0xFFFFFFFF) // --secondary-foreground: #ffffff

// Muted (subtle text, disabled states)
val Muted = Color(0xFF262626)              // --muted: #262626
val MutedForeground = Color(0xFFA3A3A3)    // --muted-foreground: #a3a3a3

// Accent
val Accent = Color(0xFF262626)             // --accent: #262626
val AccentForeground = Color(0xFFFFFFFF)   // --accent-foreground: #ffffff

// Destructive (logout, delete)
val Destructive = Color(0xFFFF453A)        // --destructive: #ff453a

// Card surfaces
val CardBackground = Color(0xFF141414)     // --card: #141414
val CardForeground = Color(0xFFFFFFFF)     // --card-foreground: #ffffff

// Border
val Border = Color(0xFF262626)             // --border: #262626

// Input
val InputBackground = Color(0xFF141414)    // --input: #141414

// Status indicators
val StatusGreen = Color(0xFF34C759)        // emerald-500 from design
val StatusGreenDim = Color(0x1A34C759)     // emerald-500/10 for backgrounds

// Warning/high priority
val Warning = Color(0xFFFF9500)            // orange for high priority

// Logout button background tint
val DestructiveDim = Color(0xFF1A1111)     // bg-[#1a1111] from design
